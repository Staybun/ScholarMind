spring:
  datasource:
    url: ${SCHOLARMIND_MYSQL_URL:jdbc:mysql://localhost:3306/scholarmind?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai}
    username: ${SCHOLARMIND_MYSQL_USERNAME:root}
    password: ${SCHOLARMIND_MYSQL_PASSWORD:}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

